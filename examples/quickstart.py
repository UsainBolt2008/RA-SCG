"""Minimal API sketch; replace `bridge` and tensors with your own inputs."""
from ra_scg import RASCGAttack

# attack = RASCGAttack(bridge, nltk_data='./.nltk_data')
# result = attack.run_batch(clean_images, clean_caption_groups, frozen_adversarial_caption_groups)
# adv_images = result['adv_images']
